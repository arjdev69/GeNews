import styled from "styled-components";

export const Box = styled.div`
  .image-container {
    position: relative;
    overflow: hidden;
    background: rgba(0, 0, 0, 0.05);
    display: block;
  }
`;
